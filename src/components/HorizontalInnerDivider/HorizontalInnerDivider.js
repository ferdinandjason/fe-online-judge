import React from 'react';

import Styles from './HorizontalInnerDivider.scss';

const HorizontalInnerDivider = (props) => <hr className={Styles.HORIZONTAL_INNER_DIVIDER} {...props}/>;

export default HorizontalInnerDivider;